# =============================================================================
# AI Todo Application - Vercel Environment Variables
# =============================================================================
# Configure these in your Vercel project settings:
# https://vercel.com/[your-team]/[your-project]/settings/environment-variables
#
# NEVER commit actual values to version control
# For local development, copy this file to .env.local

# =============================================================================
# DATABASE (REQUIRED)
# =============================================================================
# Supabase PostgreSQL connection strings
# Get these from: Supabase Dashboard > Project Settings > Database

# Pooled connection - Use for application queries (connection pooling via PgBouncer)
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
# REQUIRED for production
DATABASE_URL=

# Direct connection - Use for migrations only (bypasses connection pooler)
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
# REQUIRED for running migrations (db:migrate)
DIRECT_URL=

# =============================================================================
# AUTHENTICATION (REQUIRED)
# =============================================================================
# NextAuth.js v5 configuration

# Secret used to encrypt session tokens and CSRF tokens
# Generate with: openssl rand -base64 32
# Or use: npx auth secret
# REQUIRED
AUTH_SECRET=

# Base URL for authentication callbacks
# On Vercel: Automatically set to your deployment URL (no need to configure)
# For custom domains: Set to https://your-domain.com
# NOTE: Vercel auto-sets NEXTAUTH_URL, but you can override with AUTH_URL if needed
# AUTH_URL=

# Legacy variable name (auto-detected by NextAuth)
# On Vercel: Automatically populated - DO NOT set manually
# NEXTAUTH_URL=

# Legacy secret variable name (use AUTH_SECRET instead)
# NEXTAUTH_SECRET=

# =============================================================================
# AI CONFIGURATION (REQUIRED)
# =============================================================================
# Anthropic API Key (https://console.anthropic.com/)
# Required for AI features - task decomposition, smart suggestions, etc.
# REQUIRED
ANTHROPIC_API_KEY=

# =============================================================================
# OPTIONAL: Supabase Auth (if using Supabase Auth instead of NextAuth)
# =============================================================================
# NEXT_PUBLIC_SUPABASE_URL=https://[project-ref].supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# =============================================================================
# OPTIONAL: OAuth Providers
# =============================================================================
# Google OAuth (https://console.developers.google.com/)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# GitHub OAuth (https://github.com/settings/developers)
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# =============================================================================
# AI PROVIDERS (At least one required)
# =============================================================================
# OpenAI API Key (https://platform.openai.com/api-keys)
# Required for ChatGPT 5.x mini - fast, cost-effective AI operations
OPENAI_API_KEY=

# Default AI provider: 'openai' or 'anthropic'
# DEFAULT_AI_PROVIDER=openai

# =============================================================================
# VERCEL-SPECIFIC (Auto-populated by Vercel)
# =============================================================================
# These are automatically set by Vercel - DO NOT configure manually:
# - VERCEL (set to "1")
# - VERCEL_ENV (production, preview, or development)
# - VERCEL_URL (deployment URL without protocol)
# - VERCEL_REGION (e.g., "iad1")

# =============================================================================
# SUMMARY: Required Variables for Vercel Deployment
# =============================================================================
# 1. DATABASE_URL - Supabase pooled connection string
# 2. DIRECT_URL - Supabase direct connection string (for migrations)
# 3. AUTH_SECRET - NextAuth session encryption secret
# 4. ANTHROPIC_API_KEY - For AI features
